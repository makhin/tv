# .npmrc
# Автоматическая установка peer dependencies
auto-install-peers=true

# Строгая проверка peer dependencies (рекомендуется false для RN)
strict-peer-dependencies=false

# Публичный hoisting для React Native пакетов
public-hoist-pattern[]=*react-native*
public-hoist-pattern[]=*metro*
public-hoist-pattern[]=@react-native-community/*
public-hoist-pattern[]=@react-navigation/*

# Отключаем symlinks для лучшей совместимости с Metro
node-linker=hoisted

# Модули в shamefully-hoist для решения проблем с React Native
shamefully-hoist=true