import { defineConfig } from 'orval';

// ПРИМЕР конфигурации Orval с Fetch вместо Axios
export default defineConfig({
  photobank: {
    input: {
      target: './openapi.yaml',
    },
    output: {
      mode: 'tags-split',
      target: './src/api/generated',
      client: 'react-query',
      baseUrl: '/api',
      override: {
        mutator: {
          path: './src/api/client-fetch.ts',
          name: 'customFetch',
        },
        query: {
          useQuery: true,
          useMutation: true,
          signal: true,
        },
      },
      prettier: true,
      clean: true,
    },
  },
});
